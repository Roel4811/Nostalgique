<div class="container">
  <% @artists.each do |artist| %>
    <div class="artist-card">
      <%= link_to artist_path(artist) do %>
        <h3><%= artist.name %></h3>
      <% end %>
    </div>
  <% end %>
</div>
